(function (app) {
  'use strict';

  app.registerModule('comments');// The core module is required for special route handling; see /core/client/config/core.client.routes

}(ApplicationConfiguration));

