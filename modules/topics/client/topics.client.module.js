(function (app) {
  'use strict';

  app.registerModule('topics');// The core module is required for special route handling; see /core/client/config/core.client.routes

}(ApplicationConfiguration));

